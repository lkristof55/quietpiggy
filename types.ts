export interface NavItem {
  label: string;
  href: string;
}

export interface NewsItem {
  text: string;
  urgent: boolean;
}

export interface TokenData {
  name: string;
  value: number;
  fill: string;
}
